# Environment Variables — ClearClause

# Copy this file to frontend/.streamlit/secrets.toml and fill in real values.
# NEVER commit the real secrets file — it is gitignored.

# Required
OPENROUTER_API_KEY=your-openrouter-api-key-here

# HuggingFace embeddings (all-MiniLM-L6-v2) run locally — no API key needed.
